import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appmusic.com',
  appName: 'platzi-music',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
